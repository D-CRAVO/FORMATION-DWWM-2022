ctrl F 
extension_dir = "ext"
--> la décommenter

ctrl F
_pdo

extension=curl

extension=intl

extension=mbstring

extension=openssl

extention=pdo_mysql
extension=pdo_pgsql
extension=pdo_sqlite
--> les décommenter ou les taper
